interface:
  display_name: "EvalMe Drill"
  short_description: "Drill: host a mock interview from the task bank (prompt-only), save the transcript, record the trial"
  default_prompt: "Use $evalme-drill to run a mock interview on a task from my bank and record it."
